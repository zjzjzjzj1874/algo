package base

// 快排，类似荷兰国旗问题，随机取一个数a，把数据分为 三部分， <a     =a     >a
func quick(arr []int, l, r int) []int {
	if l < r {

	}

	return arr
}

// 分区：随机取一个数a，把数据分为 三部分， <a     =a     >a,返回的数组是两部分的分界角标
func partition(arr []int, l, r int) []int {
	small := l - 1
	big := r

	for small < big {

	}

	return arr
}
